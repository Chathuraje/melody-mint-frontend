import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#1D4ED8",
    },
    secondary: {
      main: "#1D4ED8",
    },
    error: {
      main: "#E73A3A",
    },
  },
});
